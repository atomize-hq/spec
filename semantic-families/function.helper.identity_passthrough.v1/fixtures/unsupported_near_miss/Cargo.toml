[package]
name = "function-helper-identity-passthrough-unsupported-near-miss"
version = "0.1.0"
edition = "2024"

[dependencies]
rust_decimal = { version = "1.41", features = ["serde"] }

[workspace]
