# Acceptance

- Confirm the run basis and record any basis override in `session-log.md`.
- Seed `spec-cli/tests/rust_v1_closure.rs` with parent-owned helpers and lane markers.
- Create lane-local closure fixture subtree scaffolding under `spec-cli/tests/fixtures/benchmarks/rust_v1_closure/`.
- Create bounded task packets under `.runs/i5/task/**`.
- Commit the scaffold before any integration or worker worktree is created.
