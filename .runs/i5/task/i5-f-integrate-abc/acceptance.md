# Acceptance

- Reject any lane that edits outside its write set or outside its named suite section.
- Reject any pre-Lane-D edits to committed shared benchmark fixtures or snapshots.
- Run the Gate 1 command wall and launch Lane D only if the live wall is stable and readability/snapshot refresh is the only remaining drift.
